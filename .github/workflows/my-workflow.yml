name: My First Workflow
on:
   workflow_dispatch:

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Hello cats
              run: echo "The first command"
            - name: Checkout 
              uses: actions/checkout@v5
              
            - name: Hello dogs
              run:  | 
                  cat README.md
                  sudo mkdir /cats
                  echo "cats are the best" >> /cats/cat.txt
                  chmod 644 cats/cat.txt
                  ls /cats

            