name: My First Workflow
on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Hello cats
              run: echo "The first command"
            - name: Checkout 
              uses: actions/checkout@v5
              
            - name: Hello dogs
              run:  | 
                    cat README.md
                    sudo mkdir /cats
                    echo -e "\ncats are the best" | sudo tee /cats/cat.txt
                    ls -l /cats

            - name: Upload cat.txt
              uses:  actions/upload-artifact@v4
              with: 
                 name: Downloaded_cat.txt    # new file name we want to have
                 path: /cats/cat.txt               # Path to the existing file

            - name:  Executing Shell
              run : |
                     pwd
                     ls -la
                     sudo chmod +x file.sh
                     ls -l
                     ./file.sh

    second_job: 
        needs: first_job
        runs-on: ubuntu-latest
        steps:
             - name: Checkout 
               uses: actions/checkout@v5
             
             - name: Cat job
               run:  | 
                    ls -la
                    sudo chmod +x cat.sh
                    ls -la
                    ./cat.sh
                    sudo mkdir /tiger
             - name: Download cat.txt
               uses: actions/download-artifact@v5
               with:
                 name: Downloaded_cat.txt
                 path: /tiger   # the path we want our file to be saved

             - name: Read file
               run: cat /tiger/Downloaded_cat.txt
               
              

            