name: My First Workflow
on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Hello cats
              run: echo "The first command"
            - name: Checkout 
              uses: actions/checkout@v5
              
            - name: Hello dogs
              run:  | 
                    cat README.md
                    sudo mkdir /cats 
                    echo -e "\ncats are the best" | sudo tee /cats/cat.txt
                    ls -l /cats

            - name:  Executing Shell
              run : |
                     pwd
                     ls -la
                     sudo chmod +x file.sh
                     file.sh
              

            